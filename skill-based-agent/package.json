{
  "name": "skill-based-agent-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "bun run --cwd apps/skill-based-agent start",
    "typecheck": "bun run --cwd packages/protean typecheck && bun run --cwd apps/skill-based-agent typecheck",
    "lint": "bun run --cwd packages/protean lint && bun run --cwd apps/skill-based-agent lint",
    "test": "bun run --cwd apps/skill-based-agent test",
    "test:integration": "bun run --cwd packages/protean test:integration"
  }
}
